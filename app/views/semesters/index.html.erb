</p>

<div class="input-group">

  <%= form_tag("/courses", method: "get") do %>
      <%= label_tag(:code, "Type in course name:") %>
      <%= text_field_tag (:code), "e.g. 'CS1101S'", class: 'form-control' %></p>
      <span class="input-group-btn">
        <%= submit_tag "Search", class: 'btn btn-primary', disabled: true %>
      </span>
      <span class="input-group-btn">
        <%= submit_tag "+", class: 'btn btn-primary', disabled: true %>
      </span>
  <% end %>
</div>

<div>

</p>
<b><%= @course.code %></p></b>
<% @semesters.each do |semester| %>
	<%= link_to semester.number, semester_papers_path(semester.id), method: :get, class: 'btn btn-primary' %>
<% end %>

</div>